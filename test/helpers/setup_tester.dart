import 'package:flutter/material.dart';

class SetupTester extends StatelessWidget {
  const SetupTester({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
